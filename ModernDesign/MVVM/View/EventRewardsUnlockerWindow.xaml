<Window x:Class="ModernDesign.MVVM.View.EventRewardsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="http://schemas.microsoft.com/netfx/2009/xaml/presentation"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="av" 
        Title="Leuan | Event Rewards Unlocker" 
        Height="600" 
        Width="850"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen" 
        Icon="/Icono_TLK.ico">

    <Window.Resources>
        <!-- Primary Button Style -->
        <Style x:Key="ActionButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#6366F1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" 
                                CornerRadius="12" Padding="20,14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#818CF8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bg" Property="Background" Value="#404040"/>
                                <Setter Property="Foreground" Value="#808080"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Status Card Style -->
        <Style x:Key="StatusCard" TargetType="Border">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" ResizeBorderThickness="6" 
                            CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="16">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F172A" Offset="0"/>
                <GradientStop Color="#1E1B4B" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Border Grid.Row="0" Padding="25,15">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="BackBtn" Width="40" Height="40"
                                Background="Transparent" Foreground="White" 
                                BorderThickness="0" Cursor="Hand"
                                Click="BackBtn_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border x:Name="bg" Background="Transparent" CornerRadius="10" Padding="8">
                                        <TextBlock Text="←" FontSize="20" FontWeight="Bold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#20FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <TextBlock x:Name="HeaderTitle" Text="🎁  Event Rewards Unlocker" Foreground="White" FontSize="18" 
                                   FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0,0,0"/>
                    </StackPanel>

                    <Button x:Name="CloseBtn" HorizontalAlignment="Right" Width="40" Height="40"
                            Background="Transparent" Foreground="White" BorderThickness="0" 
                            Cursor="Hand" Click="CloseBtn_Click"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Template>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border x:Name="bg" Background="Transparent" CornerRadius="10" Padding="8">
                                    <TextBlock Text="✕" FontSize="18" FontWeight="Bold"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="#EF4444"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- MAIN CONTENT -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                          WindowChrome.IsHitTestVisibleInChrome="True">
                <StackPanel Margin="35,5,35,35">

                    <!-- CREDITS & INFO -->
                    <Border Style="{StaticResource StatusCard}">
                        <StackPanel>
                            <TextBlock x:Name="InfoTitle" Text="Unlock Unique Event Items" 
                                       Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

                            <TextBlock x:Name="DescriptionText" Foreground="#CBD5E1" FontSize="14" TextWrapping="Wrap" LineHeight="22">
                                This tool installs a mod that modifies game headers to unlock all limited-time event rewards in The Sims 4.
                                <LineBreak/><LineBreak/>
                                This mod was created by <Span FontWeight="Bold" Foreground="#818CF8">Seijan</Span>. 
                                All credits for this utility go to them for helping the community unlock everything!
                            </TextBlock>

                            <Button x:Name="PatreonBtn" 
                                    Content="Visit Seijan's official Patreon" 
                                    Background="Transparent" Foreground="#818CF8" 
                                    FontWeight="Bold" FontSize="13"
                                    BorderThickness="0" HorizontalAlignment="Left" 
                                    Margin="0,15,0,0" Cursor="Hand" 
                                    Click="PatreonBtn_Click"/>
                        </StackPanel>
                    </Border>

                    <!-- STEP: SELECT MODS FOLDER -->
                    <Border Style="{StaticResource StatusCard}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="40" Height="40" 
                                        Background="#6366F1" CornerRadius="20"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="📁" FontSize="18" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                                    <TextBlock x:Name="FolderStepTitle" Text="Select your Mods Folder" 
                                               Foreground="White" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock x:Name="FolderStepDesc" 
                                               Text="Choose the folder located in Documents/Electronic Arts/The Sims 4/Mods" 
                                               Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Grid>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Background="#20FFFFFF" CornerRadius="10" 
                                        Padding="15,12" BorderBrush="#6366F1" BorderThickness="2">
                                    <TextBlock x:Name="ModsPathText" 
                                               Text="📂 Click 'Browse' to select your Mods folder..." 
                                               Foreground="#94A3B8" FontSize="13"
                                               VerticalAlignment="Center" 
                                               TextTrimming="CharacterEllipsis"/>
                                </Border>

                                <Button x:Name="BrowseBtn" Grid.Column="1" 
                                        Content="Browse" Margin="12,0,0,0"
                                        FontSize="14" Height="46"
                                        Style="{StaticResource ActionButton}" 
                                        Click="BrowseBtn_Click"
                                        WindowChrome.IsHitTestVisibleInChrome="True"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- ACTION BUTTON -->
                    <Button x:Name="UnlockBtn" 
                            Content="🚀  Unlock All Event Rewards!" 
                            Height="60" FontSize="17" FontWeight="Bold"
                            Style="{StaticResource ActionButton}" 
                            Click="UnlockBtn_Click" 
                            IsEnabled="False"
                            WindowChrome.IsHitTestVisibleInChrome="True" 
                            Margin="0,5,0,0"/>

                    <TextBlock x:Name="StatusLabel" Text="" 
                               Foreground="#22C55E" FontSize="13" FontWeight="SemiBold"
                               HorizontalAlignment="Center" Margin="0,15,0,0" 
                               Visibility="Collapsed"/>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>